i want a demo of a django app with a dynamo db backend

write the django db as a new backend provider

it can be opnionoated

as a test harness have aws local stack 

make the code for the dynamo db provider all in one library

write test cases